pipeline {
    agent {label 'slave3'}
    stages {
        stage('Run Python Script') {
            steps {
                // Use python3; many modern Linux images do not provide a "python" command
                sh 'python3 --version'
                sh 'python3 python_scripts.py'
            }
        }
    }
}